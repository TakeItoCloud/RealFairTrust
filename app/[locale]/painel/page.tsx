// Agent dashboard — /painel (PT) · /en/dashboard (EN)
// Access: authenticated agent. Auth guard built in Phase 5.
export default function PainelPage() {
  return (
    <main>
      <h1>Painel</h1>
      <p>Scaffold — Phase 5 (auth + listings)</p>
    </main>
  )
}
