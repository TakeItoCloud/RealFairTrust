// Admin — rating oversight (recompute status, flags, health) — /admin/avaliacoes
export default function AdminAvaliacoesPage() {
  return (
    <main>
      <h1>Admin — Avaliações</h1>
      <p>Scaffold — Phase 5 (rating engine)</p>
    </main>
  )
}
