# Decisions Log — RealFairTrust

> Locked choices only. One line each. If a decision changes, add a new dated line; don't
> delete history.

| # | Date | Decision | Notes |
|---|------|----------|-------|
| 1 | 2026-05-28 | Brand name is **RealFairTrust** | Replaces old prototype name "Agentra" |
| 2 | 2026-05-28 | Treat as a **real launch**, **city-limited** MVP | Assumed Lisbon + Porto — to confirm |
| 3 | 2026-05-28 | **Hosts property listings** (buy/rent/sell) inside MVP | Sequenced build, schema-first, additive only |
| 4 | 2026-05-28 | Revenue: **commission split + optional agent Pro subscription**; clients free | |
| 5 | 2026-05-28 | Rating: **rolling 90-day window, recomputed monthly** | Not cumulative |
| 6 | 2026-05-28 | Rating: **per-opportunity normalization** + **"Rising Talent" board** (<6 mo) | Fairness for newcomers |
| 7 | 2026-05-28 | Accounts: **agent + admin first**; client accounts later; clients browse/contact without account | |
| 8 | 2026-05-28 | Keep the **gold-on-dark luxury** brand direction and a hero-led homepage | Carry equity from prototype |
| 9 | 2026-05-28 | Stack: **Next.js 15 / React 19 / TS / Tailwind 4 / Framer Motion / next-intl / Supabase**; pnpm | Enhance, don't replace |
| 10 | 2026-05-28 | Infra: remote Linux `192.168.16.11:/projects/RealFairTrust`, GitHub repo, **Vercel** deploy | Branch model main→develop→feat/* |
| 11 | 2026-05-28 | Deliverables per phase: high-level **.md + branded Word doc**, with recommended options | Stop & ask before each next phase |
| 12 | 2026-05-29 | **Logo: Concept C (Verified Roofline)** primary; **Concept B (Seal)** = in-product "Verified" badge | Refined in Phase 3 |
| 13 | 2026-05-29 | **Launch cities: Lisbon + Porto** | New cities = data + feature flag (see notes/city-expansion.md) |
| 14 | 2026-05-29 | **Listings source: agents only at launch** | Owner-direct revisited later |
| 15 | 2026-05-29 | **Language: PT primary, EN secondary**; PT default on first visit | next-intl |
| 16 | 2026-05-29 | **Rating weights** 35/25/15/15/10 (satisfaction/close/responsiveness/conversion/activity) | Confirmed |
| 17 | 2026-05-29 | **Phase 0 APPROVED** | Proceed to Phase 1 on user go-ahead |
| 18 | 2026-05-29 | **Score display:** badges + "building track record" first; reveal numeric score once sample is statistically fair | |
| 19 | 2026-05-29 | **Seller lead routing:** client picks from top matching local consultants + "suggest for me" option | |
| 20 | 2026-05-29 | **Listings at launch:** seed a curated, clearly-flagged set per launch city until real inventory builds | |
| 21 | 2026-05-29 | **Locale URLs:** localized pathnames + hidden default locale (PT) | next-intl |
| 22 | 2026-05-29 | **Consultant slug:** name-based, numeric suffix on collision | Applied as recommended default |
| 23 | 2026-05-29 | **Reviews at launch:** collect now with manual moderation; verification gating in Phase 2 | |
| 24 | 2026-05-29 | **Phase 1 APPROVED** | Proceed to Phase 2 (Wireframes) on user go-ahead |
| 25 | 2026-05-29 | **Hero:** conversion-first split (value + inline search left); contained media panel right — poster-first, optional muted looping video on idle (off under reduced-motion), with a "Top this month" proof card | Keeps video safely; see `brand/wireframes/wf-home-hero-recommended.svg` |
| 26 | 2026-05-29 | **Leaderboard:** card grid + "Top this month" badges + subtle rank in Ranked view | |
| 27 | 2026-05-29 | **ConsultantCard:** badges + close rate + specialization (full sub-signal breakdown on profile) | |
| 28 | 2026-05-29 | **Contact form:** inline on desktop; sticky button → full form on mobile | |
| 29 | 2026-05-29 | **Dashboard nav:** left side-nav | |
| 30 | 2026-05-29 | **Phase 2 APPROVED** | Proceed to Phase 3 (Visual Design) on user go-ahead |
| 31 | 2026-05-29 | **Visual direction: D1 Midnight Gold** (hybrid: dark marketing + warm light content + verified-green) | See brand/design/direction-1 |
| 32 | 2026-05-29 | **Type: Fraunces (display) + Inter (UI/body)** | next/font/google |
| 33 | 2026-05-29 | **Theme: dark-first marketing + warm light content sections** | |
| 34 | 2026-05-29 | **Keep the verified-green accent** for verified/success only | |
| 35 | 2026-05-29 | **Phase 3 APPROVED**; tokens final (`design-tokens.css` + `tailwind-theme.css`) | Proceed to Phase 4 (Frontend Build) on user go-ahead |
| 36 | 2026-05-29 | **Phase 4: Radix primitives** for Dialog/Dropdown/Tabs/Select; bespoke Tailwind elsewhere | a11y without a heavy UI kit |
| 37 | 2026-05-29 | **Phase 4: Framer Motion** for subtle, reduced-motion-safe motion | |
| 38 | 2026-05-29 | **Phase 4: hero media = optimized poster only** for now; add video loop with real footage | |
| 39 | 2026-05-29 | **Phase 4: build dashboard + admin + auth as UI shells now** (mock data; real auth Phase 5) | |
| 40 | 2026-05-29 | **Phase 4: generate ~12 consultants / ~24 listings seed set** (Lisboa+Porto, incl. Rising Talent) | |
| 41 | 2026-05-29 | **Phase 4 build GREENLIT** — start milestone 4.0 (Foundations) | feat/* → PR → preview → review → merge per milestone |
| 42 | 2026-06-07 | ~~Dark theme retuned to deep sea-navy with radial centre glow~~ | **Superseded by #43** |
| 43 | 2026-06-12 | ~~Background "Sapphire twilight" (darkened) interim values~~ | **Folded into #45** |
| 44 | 2026-06-12 | ~~Accent "Polished foil" gold interim values~~ | **Folded into #45** |
| 45 | 2026-06-12 | **Finalized visual system (Claude Design).** Background = Sapphire twilight `radial-gradient(ellipse 90% 70% at 50% 30%, #122a4f, #0a1a34, #060f22, #020812)`, base #0A1A34, edge #020812, cards #10233C + white-0.07 hairline. Gold: **`.gold-title`** clip gradient (`#ffe6a0→#e3a812`, solid #e3a812 fallback) on titles/logo/prices; **`.btn-gold`** (`#ffe79e→#dca233`, text #2A1D04, inset+drop shadow, hover lift); solid companion **#E3A812** for small pills/badges/chips/borders/icons; labels #E3A812 on navy / **#8C5E12** on ivory (darkened from #d19e1d for AA). Three-tone wordmark: Real #F5F1EA · Fair gold-title · Trust #8A93A3. Type scale (Fraunces hero72/section40/subsection26; Inter lead20/body16/meta13/eyebrow12/button15). Card tokens: radius 22, pad 26, lift −4/−5, img-zoom 1.06, accent-bar, overlay-scrim, plate-bg, surface/raised shadows. **ConsultantCard = "Spotlight"** (horizontal, rank coin + ringed avatar, close-rate hero, #18 honored). **PropertyCard = "Editorial Overlay"** (photo-first, scrim, frosted chips, gold price, icon spec row — energy cert is a NEUTRAL muted label, green stays reserved for verified/success per #34, agent mini-avatar). Framer Motion entrance + hover, reduced-motion-safe. | Supersedes #42–44; cream #F5F1EA + verified-green #3FB984 retained; warm light #FBF8F2 unchanged; AA re-verified (cream/navy 15.4:1, gold/navy 8.2:1, button text 7–10:1, ivory label 5.3:1) |
| 46 | 2026-06-21 | **Design hand-off adopted as authoritative visual spec.** The "RealFairTrust Design System" bundle (`design/handoff/`) supersedes earlier ad-hoc mockups. It is the fully-specified form of #45 (same navy/gold/Fraunces+Inter system) plus the ivory light section, the gold bright/calm split, the full token set, and the Framer-Motion interaction spec. | Reference/spec only — not shipped. See `docs/PROJECT-STATE.md` §7 |
| 47 | 2026-06-21 | **Icons: `lucide-react`** (2px stroke), replacing inline SVGs | |
| 48 | 2026-06-21 | **Fonts via `next/font/google`** (Fraunces + Inter); self-hosting woff2 optional later | Not the bundle's `@import` |
| 49 | 2026-06-21 | **PropertyCard energy certificate stays NEUTRAL/muted**, **overriding** the hand-off's "verified-green" instruction | Green reserved for verification only; upholds #34 |
| 50 | 2026-06-21 | **Token reconciliation done carefully** (alias old→new or migrate references) to keep the build green with least churn | Applied across design-apply run order |
| 51 | 2026-06-22 | **DESIGN AUTHORITY (supremacy rule).** The Claude Design export (`design/handoff/`, from `RealFairTrust_Design_System.zip`) is the **supreme authority for all visual/design values** and supersedes any conflicting prior decision — **EXCEPT** where adopting the zip's literal value would damage **accessibility (WCAG AA)** or **performance**, in which case the AA/perf-safe deviation is retained and documented as an explicit, justified exception (see #53). When the zip self-contradicts, its `README.md` self-declares authority and wins. | Generalises #46. Governs the whole design-apply run order |
| 52 | 2026-06-22 | **ENERGY CERT renders GREEN (SUPERSEDES #49; explicit exception to #34).** The PropertyCard energy certificate renders in the verified-green per the zip (matches the EU/PT energy-label convention). The verified-green token is therefore **no longer verification-exclusive** — it also denotes energy rating on the PropertyCard. Implemented in run-order **Step 4**. | Reverses #49 (which had kept it neutral). Future note: if the dual meaning of green becomes a problem, the collision-free option is the EU A→G colour scale on that badge |
| 53 | 2026-06-22 | **AA/PERF EXCEPTIONS RETAINED under #51** (zip literals NOT adopted because they damage AA/perf; ratios measured against real rendered backgrounds, not estimated): **(a)** Ivory eyebrow/label gold stays **#8C5E12** — measured **5.32:1** on ivory `#fbf8f2` (AA pass); the zip's `#d19e1d` is only **2.30:1** (fail). **(b)** Verified-ink-on-light becomes **#157048** — measured **5.22:1** on the real badge background (`bg-verified/12` over ivory) and **6.10:1** on white (AA pass); the interim **#1E8F62** measured only **3.48:1** on that background (fails AA 4.5 for the labelled pill text; clears only the 3:1 icon bar) and the zip's `#2f9a6c` only **3.01:1** — both superseded. Token swap implemented in **Step 3**. **(c)** Fonts via **`next/font/google`** (#48), not the zip's `tokens/fonts.css` `@import` — same fonts, better perf, App-Router-safe. **(d)** Type scale follows the zip's **README** (hero **72** / section **40**), not the bundle's `tokens/typography.css` (hero 76) — the zip self-contradicts and the README self-declares authority. | Exceptions to #51. (b) supersedes the earlier "keep #1E8F62 for AA" note (which was never measured). (a)/(c)/(d) carry forward #45/#48 |
| 54 | 2026-06-22 | **Radius scale adopted project-wide** (plan §2.9, design-apply Step 3). The Tailwind `rounded-*` utilities now bind to the hand-off scale: `rounded-sm` = **6** (`--radius-xs`, unchanged) · `rounded-md` = **14** (`--radius-md`, was 10) · `rounded-lg` = **20** (`--radius-lg`, was 16). Implemented by re-pointing `--rft-r-sm/-md/-lg` → `--radius-xs/-md/-lg` (the @theme mapping is untouched). Form wells (Input/Select/Textarea) pin to **`--radius-sm` (10)** explicitly; cards keep **`--card-radius` (22)**. | Global md/lg rounding increase; cards unaffected (explicit literals). Verified in compiled CSS |
| 55 | 2026-06-22 | **Icon layer = `lucide-react` re-export shim** (implements #47). `components/ui/icons.tsx` keeps the existing `Icon*` names but each is a thin wrapper over a lucide glyph (2px stroke, `currentColor`, 1em box) so every consumer is unchanged; the canonical lucide set (README §5) is also exported as `Icon*` wrappers for new code. Inline SVGs removed from `icons.tsx`. | Chosen over per-consumer migration (13 icons across 9 files). Header's animated hamburger + Modal/MediaImage inline SVGs left as-is |
| 56 | 2026-06-22 | **Home section rhythm — canonical** (ratifies the Step-5 proposal, design-apply Step 6). The Home page alternates navy-stage and warm-ivory bands: **N · I · N · N · I · N · N** — 1) Hero navy radial · 2) **HowItWorks IVORY** · 3) Top consultants navy · 4) Featured listings navy · 5) **Clients/consultants split IVORY** (white Card "ivory" panels) · 6) Trust band navy · 7) Join CTA navy (signature gold-on-dark close). | Keeps the dark-first brand (#33); ConsultantCard/PropertyCard sections stay on the navy stage (frosted cards float on the radial); two ivory breaks bracket the card showcase. AA on ivory verified (#53 inks). **SUPERSEDED by #61 (champagne rhythm)** |
| 57 | 2026-06-24 | **DESIGN REVISION adopted** — the new, revised Claude Design export (`design/handoff/`, the champagne revision) is the **supreme visual source of truth** for this revision; README wins on bundle self-contradiction. Generalises/extends #51; governs the R-series (R2→R5). | Ratified at the end of the revision run order (R5). Implemented on the `chore/design-revision-*` chain, **unmerged** (main/develop frozen until promotion) |
| 58 | 2026-06-24 | **Background — new radial.** `--bg-navy-radial` = `radial-gradient(ellipse 89% 81% at 50% 48%, #1e4680 0%, #173a63 33%, #0e2545 59%, #081830 81%, #040e20 100%)`. **Supersedes the #45/#46 background** (ellipse 90% 70% at 50% 30%, #122a4f…). | Brighter, re-centred centre `#1e4680` = the AA worst case → drove the #64 fixes. Implemented R2 |
| 59 | 2026-06-24 | **Gold — symmetric 90° gradients.** Title `linear-gradient(90deg,#d8950f,#e3a812 16%,#ffe6a0 50%,#e3a812 84%,#d8950f)`; button `linear-gradient(90deg,#c8901f,#e9bb52 28%,#ffe79e 50%,#e9bb52 72%,#c8901f)` (+ hover). **Supersedes the #45 180°/160°.** README §2 `.btn-primary` hover inconsistency resolved to the §1.2 **token** (90°). | Gradient `#e3a812` stops are literals (kept). Implemented R2 |
| 60 | 2026-06-24 | **Type scale — kit scale.** Hero **76** (`--fs-display-1`) / section **42** (`--fs-h1`) / CTA **56** (`--fs-display-2`) + kit name aliases (`--fs-h2/3/4`, `--font-display/-body/-ui`, `--fw-*`). **SUPERSEDES #53(d)** (README 72/40). | `--fs-hero`/`--fs-section` changed in place; kit names aliased (no component renames). Implemented R2 |
| 61 | 2026-06-24 | **Champagne family** (`--champagne #ece2cb`, `-card #fbf7ee`, `-border #e3d7bd`, `-ink #2b2415`, `-ink-muted #5c5340`, `-eyebrow #7c5a12`) + `.rft-champagne`/`.rft-step-card`/`.rft-step-coin`. Used **ONLY** on the Home "Como Funciona" band + the footer; navy+gold default elsewhere. **SUPERSEDES the #56 ivory rhythm.** | Ivory tokens/helpers retained but unused (later-cleanup candidate). Tokens R2; wired R4 |
| 62 | 2026-06-24 | **Home composition = the marketing kit** (`reference/ui_kits/marketing/Home.jsx`): navy hero (search-pill: city Select + search Input + "Procurar" → /consultores · featured ConsultantCard · floating "+6 lugares" stat) · **champagne** HowItWorks step-cards · navy Leaderboard · navy FeaturedProperties · navy gold-bordered AgentCTA · **champagne footer**. The **"Clients/consultants split" + "Trust band" sections are REMOVED** (orphaned i18n keys deleted). | Ships with our next-intl strings (pt+en), seed data (#20), lucide icons (#47/#55), components. Implemented R4 |
| 63 | 2026-06-24 | **Component alignment (R3).** Button = **pill** (`--radius-pill`, handoff §6.1/§2) — a11y tap targets (`min-h-11/13`) retained over the handoff's literal padding; `Select` options accept `(string \| {value,label})[]`; `Eyebrow tone="champagne"`. **ConsultantCard keeps its name** with the AgentCard "Spotlight" spec (#G); **`onIvory` ≡ our `onLight`** (no rename). Fonts via `next/font` (#48) + lucide shim (#47/#55) unchanged. | a11y > literal pixel where they conflict. Implemented R3 |
| 64 | 2026-06-24 | **AA/a11y exceptions ratified** (measured vs the REAL composited bg under the new brighter stage; **fail-closed**, no global text token lowered): **(a)** muted raised .58→**.70** (4.66, worst frosted composite @ `#1e4680`); **(b)** navy small-gold/eyebrow `--gold-500` #e3a812→**#efb52a** (5.04) at the **shared** solid-gold token (~10 small-gold consumers shared the failure) — gradient stops kept #e3a812; **(c)** champagne-eyebrow #a9791a→**#7c5a12** (4.90); **(d)** ivory-label **#8C5E12** kept (5.32 on ivory; moot on champagne, which uses #7c5a12); **(e)** on-light verified-ink **#157048** carried (4.74 champ / 6.10 white, #53); **(f)** hero featured card + floating stat moved **raised(.06)→default(.035)** fill (muted 4.38→4.66; featured keeps gold border/glow/accent); **(g)** on-DARK green small **TEXT** → new **`--green-verified-strong #5fd2a1`** (4.58 @centre; #3fb984 had dropped to 3.46/2.97) for energy cert + StatBlock delta — #3fb984 kept for icons/seals/large/accents; **(h)** verified pill + Badge gold/rising/success (navy) → **solid dark chip** (`--surface-card-solid`) so the bright stage can't bleed (green 6.78, gold 9.05). | (g)/(h) discovered by the R5 global sweep. Symmetric with #53's on-light ink. onIvory chip paths unchanged. Implemented R2/R4/R5 |
| 65 | 2026-06-25 | **HOME-PAGE VIDEO REVISION adopted** — the new Home handoff (`design/handoff-home/`, the `design_handoff_home` bundle) is the **supreme visual source of truth for the Home page**, extending #57/#51 (README wins on self-contradiction). The **full-bleed cinematic video hero is the DEFAULT** (`heroMode="Vídeo full-bleed"`, 84vh viewport breakout under the sticky nav), **superseding the R4 search-pill marketing-kit Home (#62)**. The contained-panel hero mode (handoff §3.5) is kept in the **design reference only — not built**. | Mostly composition + a new HeroMedia component; the token system is already in place from R2–R5 (#57–#64). Implemented RH1→RH4 on the `chore/design-revision-home-*` chain, **UNMERGED**. Ratified at RH5 |
| 66 | 2026-06-25 | **Hero headline + Home-specific clamp.** Headline = "O futuro do imobiliário" (cream line 1) / "está em cada um de nós." (**gold-gradient italic** line 2), Fraunces. The full-bleed hero h1 uses a **Home-specific clamp `clamp(38px,4.5vw,62px)` (~max 62px)** — a literal in the hero component, **distinct from the `--fs-display-1` 76 display token** (#60), which is unchanged. | i18n via `hero.line1`/`hero.line2` (PT+EN). Not a token change. Implemented RH4 |
| 67 | 2026-06-25 | **Staged hero entrance (~2.6s) with the EXPORT-SAFETY CONTRACT.** Foreground cluster reveals line1 0ms → gold line2 700 → rule 1250 → sub 1700 → buttons 2100 (ease `cubic-bezier(.2,.62,.2,1)`). **Base state is VISIBLE by default**; motion is applied at runtime and gated on `useReducedMotion()` — **SSR / no-JS / reduced-motion show the final layout instantly, never a blank hero** (verified: zero `opacity:0` in the hero SSR HTML). | `HeroFullBleed` uses Framer `useAnimationControls` with no `initial`; hidden set pre-paint, client-only. #37 honored. Implemented RH3b |
| 68 | 2026-06-25 | **Real/Fair/Trust brand reveal.** Three beats (Real/Fair/Trust) crossfade every **3000ms** (gold-gradient word ~56px + cream phrase ~21px) behind their **own soft radial scrim**, bottom-right, with **`startDelay 2750ms`** so they hold until the entrance finishes. Reduced-motion → static stacked lines (no cycling). Phrases: "Pessoas antes de imóveis." / "Mérito, não antiguidade." / "Confiança pela transparência." | Decorative layer `aria-hidden`; text via i18n (`brand.*`). AA over the brightest frame: phrase 7.44–12.24:1. Implemented RH3b/RH4 |
| 69 | 2026-06-25 | **`--gold-on-light: #8C5E12`** — named alias of `--rft-gold-deep` (same value, #53a) for the **on-light wordmark/mark** (the bright title-gold gradient disappears on champagne/ivory). | 1-line additive alias in `app/design-tokens.css`. Consumed by the Logo/Wordmark/Footer. Implemented RH2 |
| 70 | 2026-06-25 | **Roofline-mark `Logo` built (#12 finally implemented).** Bespoke roofline-check **mark** (SVG per the handoff Logo spec) + the **tri-tone wordmark**, em-sized; wired into the Header (mark now visible — previously text-only) + Footer. On navy: mark = bright gold gradient. **On-light (`onIvory`): mark + "Fair" = solid `--gold-on-light #8C5E12`** (logotype exemption, **4.38:1** on `#ece2cb` — WCAG 1.4.3 exempts brand logotype; the mark is a graphic ≥3:1; no real body text is sub-4.5). | Concept C "Verified Roofline" (#12) realised. Real/Trust on light = dark ink (AA). Implemented RH3a |
| 71 | 2026-06-25 | **AgentCard (ConsultantCard) fill darkened to solid `--surface-card-solid #0c1d39`** (was the translucent `.035` over the bright radial); inert backdrop-blur dropped. Featured/spotlight merit → **56px** (others 38px). **AA WIN:** muted `.70` text **4.66 → 7.82:1** (solid dark vs translucent composite); body .78 9.44, strong 14.92, gold-500 9.05, green-strong 8.97. #18 gating + #37 motion preserved. | Aligns with the #64h solid-chip direction. Implemented RH3a |
| 72 | 2026-06-25 | **Slim-15px navy↔champagne fade transitions.** `.rft-champagne--fade-both` (Como-funciona band, top+bottom) and `.rft-champagne--fade-top` (footer, top only) dissolve the champagne edge to transparent over a 15px band so the navy stage shows through — **no hard line/shadow**. Only override `background`; the flat `.rft-champagne` fill is otherwise unchanged. | Additive modifiers on `.rft-champagne` (#61). Helpers added RH2, wired RH4 |
| 73 | 2026-06-25 | **"Top este mês" spotlight + row.** Featured #1 ConsultantCard (56px merit) on a **gold-glow pedestal** + a floating **"+6 lugares · últimos 90 dias"** green badge; below, a **3-card row** (#2–#4) — **equal-height cards** (`h-full` + `mt-auto` footer) with **full names (no truncation)**. Rank coins use the **GLOBAL ranked-list position via a new `displayRank` prop** (featured 1, row 2/3/4), **distinct from the per-region `score.rank`** used by the Consultores leaderboard; `showCoin` shows all four positions; **#18 gating preserved**. The **EXPLORE/EXPLORAR scroll cue is an accessible `<button>`** (native focus + Enter/Space, smooth-scroll −64px, instant jump under reduced-motion). | Replaces the R4 Leaderboard grid. Maps to OUR ranked seed (#1 = ana-silva). Badge AA 6.78:1 on `#0c1d39`. Implemented RH4 + the RH4 fixes |
| 74 | 2026-06-25 | **Hero video optimized 8.0 → 1.6 MB** (`libx264 crf24 -preset slow -pix_fmt yuv420p -an -movflags +faststart`; no upscale; poster 1.1 MB → 124 KB; original `hero-original.mp4` gitignored). VP9 WebM came out larger → dropped. **KNOWN 4.5 ITEM:** the 5s clip has a **hard loop seam (first↔last SSIM 0.22 / PSNR 8.2 dB)** — replace with a crossfade-loop or a longer clip at polish. | ffmpeg 4.4.2 on the host. Not blocking. Implemented RH3b |
| 75 | 2026-06-25 | **Footer DEVIATION recorded (fail-closed on unverified business facts, §0).** Kept the existing **4-column real-route footer** (Explore / For-consultants / Company / Legal) + the slim top fade, rather than inject the handoff §8 **3-column placeholder business contact** (Lisboa/Porto street addresses + `ola@realfairtrust.pt` + "Metodologia"). **Real contact details pending from Carlos** — adopt the handoff's 3-col footer only once real contact info is supplied. | Labels/copy adopted where a real route exists. Flagged for sign-off. Implemented RH4 |
| 76 | 2026-06-25 | **Seed photo 404s are EXPECTED** (cards fall back to initials avatars) — no placeholder images injected. **Real imagery (hero poster + property/agent photos) is a Phase 4.5 polish item** (#9 OPEN). | Upholds §0 (don't fake assets). Carries the 4.5 imagery debt. |
| 77 | 2026-07-12 | **Additive `Property.kind` field (schema-first, Hard Rule #1)** backing the discovery "Tipo" filter — distinct from `type` (the deal: sale/rent). `PropertyKind = apartment\|house\|studio\|loft\|commercial\|building\|land`; all 24 seed listings assigned a kind. The Tipo filter only offers kinds present in the seed (apartment/house/studio/commercial/building) so no option returns an empty grid. | Additive-only (no table rewrite). Carlos approved (A over deferring). loft/land reserved for future inventory. |
| 78 | 2026-07-12 | **Discovery default sort = `merit`** (attributed consultant's composite score desc; no-score agents last; ties → most-recent) — "presented by the best-rated consultants" (matches BUILD-PLAN). Options: Mérito (default) / Preço ↑ / Preço ↓. Implemented as an **additive, opt-in** `sort?: ListingSort` on `getListings` (+ additive `kind`/`minArea`/`maxArea` filters). **Guardrail (Carlos):** when `sort` is omitted, `getListings` returns exactly today's items+order (createdAt desc); **merit is NEVER the global default**. Caller audit confirmed the only two callers (Home featured row, dev showcase) pass no args → byte-for-byte unchanged. | Sort lives in the repository (correct layer, Supabase-swap-ready for Phase 5), not the page. Pure function; same return shape; no new mock data. |
| 80 | 2026-07-13 | **CAOP2025 administrative dataset adopted (standalone).** Portugal's official divisions (distrito/RA → concelho → freguesia, DICOFRE ids) sourced from **DGT** *Carta Administrativa Oficial de Portugal 2025* via DGT infogeo ArcGIS FeatureServer (Açores via the 3 per-group layers — the combined `Freguesias_RAA` blanks the Grupo Oriental DICOFRE). Stored at `lib/data/geo/pt-caop2025.json` + typed loader `lib/data/geo/caop.ts` (server-only by convention). **Counts:** 20 top-level (18 distritos + 2 RA) · 308 concelhos · 3 259 freguesias (current CAOP = post-2013 reform incl. post-2021 desagregações; NOT the ~4 260 pre-reform). **Licence CC BY 4.0**, attribute "Direção-Geral do Território (DGT)". RA top-level ids are synthetic `AC`/`MA` (no 2-digit DICOFRE exists for a RA; ilha kept in the 4-digit concelho id + `ilha` field). | **Standalone** — the existing `district→city→zone` Region model is UNTOUCHED; two models coexist. Unify/retire later (DISCOVERY-PLAN follow-up). |
| 81 | 2026-07-13 | **Additive shared-data (approved diff + caller audit).** `Property.freguesiaId` (required, CAOP DICOFRE) + `ConsultantProfile.coverageDistrictIds` (required, CAOP distrito/RA ids). `getListings` gained opt-in `distritoId`/`concelhoId`/`freguesiaId` filters (concelho = `freguesiaId.slice(0,4)`, distrito via CAOP). **Guardrail held:** the only no-arg callers (Home featured row, dev showcase) are byte-for-byte unchanged (verified). Seeds: 24 listings mapped to Lisboa 1106 / Porto 1312 freguesias; consultant coverage spread across 11/13/15/08/03/01/17 (08 Faro etc. = consultant-but-no-house fallback districts). | Only the two mock constructors build these types; tsc enforces completeness. |
| 82 | 2026-07-13 | **Location filter = Distrito → Concelho → Freguesia picker** (replaces the old Localização + Zona selects on `/comprar` + `/arrendar`). URL-synced (`?distrito/?concelho/?freguesia`), per-level type-to-search, removable per level, RFT skin. **On-demand + inventory-driven:** `/api/geo/{distritos,concelhos,freguesias}` route handlers return small inventory-filtered lists (distrito: house OR attributed consultant; concelho/freguesia: house), so the 355 KB CAOP dataset never ships to the client on first paint. Empty places never appear. | Server resolves selected names for the chips. Deal type stays route-fixed (no deal box). |
| 83 | 2026-07-13 | **Nearby fallback + area-specialist CTA + sort.** A chosen location with 0 houses → "no houses here" + widen **freguesia→concelho→distrito** (grouped by widen level; client re-sortable via the sort control) → else "other areas". **Area-specialist CTA** = the top-merit consultant whose `coverageDistrictIds` includes the selected distrito, shown near the top on a direct hit and after the fallback message. **Default sort = merit** (desc) **→ price** (asc) **→ announcement date** (newest, = `createdAt`); client can switch to price ↑/↓ or `date`. Merit stays opt-in for other callers (never a global default). | Fallback is server-computed; no pagination in fallback groups. |
| 85 | 2026-07-13 | **Property detail page + additive `getSimilarListings`.** Built `/imovel/[id]` (EN `/property/[id]`) — gallery (`PropertyGallery` over `MediaImage`), gold price (total/€mês), specs + green energy badge, description, location "Freguesia · Concelho · Distrito" (CAOP), attributed-consultant mini-card (#18 merit, links to profile), sticky lead panel (`PropertyContact` composing `LeadForm`+`Modal`, no persistence #28), similar-listings row. **Similar** = NEW opt-in `getSimilarListings(listing, limit)` — same deal type, **same concelho first then widen to distrito** (CAOP), excludes current; **`getListing` left byte-for-byte unchanged** (Carlos's call: distinct Supabase query in Phase 5, single responsibility, no redundant compute since it takes the already-loaded listing). No map (deferred). PropertyCard already links to `/imovel/[id]` → no shared-component change. | Composition only; no shared component/styling touched; Home/Consultores/profile unchanged. Reused MediaImage/LeadForm/Modal/PropertyCard/Avatar/VerifiedBadge/Badge. |
| 84 | 2026-07-13 | **DGT attribution surfaced (CC BY 4.0).** `meta.licence` in the dataset + `lib/data/geo/NOTICE.md` + an on-page `geoAttribution` line on the discovery pages: "Divisões administrativas: Direção-Geral do Território (DGT) · CAOP2025 · CC BY 4.0." The shared Footer was NOT touched (would change Home/all pages); the future Methodology/attributions page should also carry it. | Satisfies the CC BY 4.0 attribution obligation without touching shared components/Home. |
| 86 | 2026-07-14 | **Location-based consultant matching: inclusive hierarchical coverage + strict tiered widening Freguesia→Concelho→District + request-a-consultant fallback.** (Built for **Vender** `/vender`; the SAME rule will later be applied to the Consultores discovery page — during final tweaks, not now.) **(a) Coverage model (additive, Hard Rule #1):** keep `ConsultantProfile.coverageDistrictIds` byte-for-byte (all 4 callers unchanged — inventory, Buy/Rent specialist CTA, types, seed); add OPTIONAL `coverageConcelhoIds?` (CAOP 4-digit) + `coverageFreguesiaIds?` (6-digit). **Inclusive hierarchical coverage:** a district-level consultant covers every concelho/freguesia in the district; a concelho-level one covers every freguesia in the concelho. **(b) Matching (`getConsultantsByArea`, new, additive; does NOT touch `getConsultants`):** given a CAOP selection, STRICT tiered widening from the deepest selected level, most-specific-wins — **1)** exact freguesia attribution → **2)** exact concelho attribution → **3)** district = the inclusive catch-all (EVERY consultant working anywhere in the district: district-, concelho-, or freguesia-level attribution within it), presented as district-level → **4)** none → request-a-consultant CTA. First non-empty tier wins; broader tiers hidden. Matching is **attribution-only** — independent of the picker's option lists. Ranked by merit (composite desc; no-score last), returns the matched tier for the UI header. **(c) Picker coverage mode (D-V1):** additive `source: 'houses'\|'coverage'` on `lib/data/geo/inventory.ts` + `&source=coverage` on `/api/geo/*` + optional `source?` prop on `LocationPicker` (default `'houses'`). Coverage = **houses ∪ consultant-attribution** at each level (a strict superset; consultant-only areas surface without listings). **Full inclusive expansion REJECTED** (a district consultant would explode into thousands of concelhos/freguesias → picker bloat) — only specifically-attributed ids are added. Buy/Rent (`FilterBar`, no `source`) is byte-for-byte unchanged. **(d) UI (D-V2/D-V3):** `ConsultantCard` NOT modified — the per-card coverage note is rendered in a Vender-only page-level wrapper; no isDemo chip (consultants aren't demo-flagged anywhere; only listings, #20). | Additive-only; caller audit in `docs/VENDER-PLAN.md`. With the current seed, coverage-mode option lists coincide with houses mode (every concelho/freguesia attribution sits on a house-bearing area); the mechanism surfaces house-less consultant areas when such attribution exists. Two location models (CAOP + old Region) still coexist. |
| 89 | 2026-07-15 | **Termos + Privacidade ship as visibly-marked DRAFTS** pending professional legal review before launch. Both `/termos` (EN `/terms`) and `/privacidade` (EN `/privacy`) render a calm **neutral** draft-notice banner at the top ("Documento em rascunho — sujeito a revisão jurídica antes do lançamento." / "Draft document — subject to legal review before launch.") — muted treatment, **NOT** the verification-green (#34). They are structurally-sound, **GDPR-aware** drafts (RGPD rights + CNPD named; contact-intermediation platform, not a mediator), **NOT lawyer-reviewed**. Placeholder **contact / controller / retention** fields are **intentionally bracketed** (`[… a definir antes do lançamento.]`) to be filled before launch. **This is not legal advice.** **LAUNCH BLOCKER (Phase 6):** professional legal review + real contact/controller/retention values are required before go-live. `/sobre` (EN `/about`) is normal content (no banner). | Copy = final approved, verbatim, PT/EN. Server components; no shared-component styling changed; champagne band reused on `/sobre` (precedent #87). Completes Phase 4.3 public pages. |
| 87 | 2026-07-14 | **Metodologia gets its own public route** `'/metodologia'` (EN `/methodology`), added to `i18n/routing.ts` pathnames (stable route contract, Hard Rule #2 — logged). It is the **deep, linkable rating-model spec** page — the long-form, scrutable version of the engine — **deliberately separate** from the client-facing `/como-funciona` (which is the 3-step quick explainer that links out to it). Metodologia also **carries the DGT/CAOP data attribution** (CC BY 4.0; the required attribution "Direção-Geral do Território (DGT)" per `lib/data/geo/NOTICE.md`, plus a link to the licence). Wired into the **Footer "Empresa" column** (one added `FooterLink`, alongside Sobre/Contacto/Recursos; footer structure unchanged). **Also (extends #61):** champagne `.rft-champagne` bands are now used on these two **static content pages** (Como funciona steps; Metodologia §1/§3/§5), not only Home "Como Funciona" + the footer — per Carlos's explicit visual spec for the static pages. | Built on `feat/static-como-metodologia`. Both pages = server components, pure content, Reveal (client) entrances; navy/champagne alternation; AA; PT/EN parity (`comoFunciona`/`metodologia` namespaces). Final approved copy used verbatim. |
| 88 | 2026-07-14 | **Methodology states the confidence threshold QUALITATIVELY, by explicit choice.** The Metodologia page describes the "enough data for a fair measurement" / "building track record" gating **in words** ("enquanto um consultor não tiver tratado oportunidades suficientes para uma pontuação estatisticamente justa … 'A construir histórico'"), and **does not publish exact numeric thresholds** (minimum opportunities/sample size, the confidence cut-offs). Those are **Phase 5 rating-engine work** and will be published **only once locked** — committing to a number now, before the engine exists, would be a guess (§0) and hard to walk back (route/spec is a public contract). §4 reuses the real product Badge strings (`score.buildingTrackRecord` / `score.risingTalent`) so the page echoes the live UI. | Symmetric with #18 (badge-until-confident). No numeric threshold is invented anywhere in the copy. |
| 79 | 2026-07-12 | **Discovery page = ONE shared `Discovery` RSC, TWO modes** (`/comprar` sale/total · `/arrendar` rent/€mês; EN `/buying`·`/renting` — route contract unchanged). **`FilterBar` REUSED + evolved** with a `dealType` mode prop (price bands + label; route-fixed deal-type control dropped); field set Localização/Zona/Tipo/Preço/Área/Quartos + row-2 result-range & Ordenar; URL-query core kept, changes reset pagination; shared option tables in new pure `lib/listingFilters.ts`. **`PropertyCard`, `Pagination`/`UrlPagination`, `EmptyState`, `Input`/`Select`/`Button`/`Eyebrow` reused UNMODIFIED** — no shared-component styling changes; Home / Consultores discovery / Consultant profile untouched. `discovery` i18n namespace added at PT+EN parity. | Mirrors the Consultores server pattern. FilterBar's only other consumer is the dev showcase (call-site updated). All gates green (tsc/eslint/build). |
| 90 | 2026-07-15 | **Two additive DEMO consultant outcome metrics (Cycle 1/3 of the review-change set):** **units sold (rolling 12 months)** + **average time-to-sell (days)**. **(a) Model:** two OPTIONAL fields `unitsSold12mo?` / `avgDaysToSell?` on **`ConsultantSummary`** (the card's view type, beside `reviewCount`/`avgRating`) — NOT on `PerformanceScore` (its contract is a 90-day window; these are a 12-month aggregate) and NOT on `ConsultantProfile` (identity, not outcomes). Additive per Hard Rule #1; the sole constructor `summarize()` attaches them. **(b) Data = DEMO:** invented illustrative values (`demoOutcomeMetrics` in `lib/mock/scores.ts`, all 12 consultants), deliberately **non-correlated** with the composite rating (Sofia 79 → highest volume/fastest; Maria 83 outsells Ana 92; Pedro 87 slowest). Real close data deferred to **Phase 5** — same open problem as `docs/RATING-ENGINE-NOTES` (commission/transaction record). **(c) Display = opt-in + off by default:** new `ConsultantCard` prop `showMetrics` (default `false`); the metric pair renders ONLY when `showMetrics` AND both values present, as two `StatBlock size="sm"` + **one muted (NOT green, #34/#89) `score.demoValues` caption** on the pair. All 5 real-page call-sites (Home ×2, Consultores ×2, Vender ×1) pass no prop → **byte-for-byte unchanged**; only the dev showcase opts in. **NO page ranking/filtering/behaviour changed** (that is Cycles 2 Vender + 3 Consultores, later). **(d)** `devShowcase` flag gate narrowed `NODE_ENV!=='production'` → **`VERCEL_ENV!=='production'`** so the block is reviewable on the feat preview (still OFF on the real production deploy). 4 new keys in the existing `score` i18n namespace, PT/EN parity. | Built on `feat/card-metrics`. Gates green (tsc/eslint/build). Verified via `pnpm start`: real pages 0 metric markup; showcase renders the 6 non-correlated demo cards. Caller audit in `docs/CARD-METRICS-PLAN.md`. |
